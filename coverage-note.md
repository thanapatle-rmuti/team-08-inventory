# บันทึกการวิเคราะห์ Coverage (ขั้นที่ 5)

## 1. บรรทัดไหนที่ test ยังไม่เคยรันผ่านเลย และการที่มันหลุดไป เสี่ยงอย่างไร

จากการรันคำสั่ง `pytest --cov=. --cov-report=term-missing` พบส่วนที่ยังไม่ถูกทดสอบ (Missing Lines) ดังนี้:

1. **`src/service.py` บรรทัด 65-69 (`receive`) และ 75-86 (`issue`)**:
   - **รายละเอียด:** เมธอดบันทึกการรับสินค้าเข้าคลัง และการจ่ายสินค้าออกเดิม
   - **ความเสี่ยง:** หากฟังก์ชันรับเข้ามีข้อผิดพลาด เช่น คำนวณสต็อกบวกผิด หรือสร้างรายการ `StockTransaction` บันทึกประวัติผิดพลาด จะทำให้ยอดคงเหลือทางบัญชีไม่ตรงกับสินค้าจริงในโกดัง
2. **`src/service.py` บรรทัด 41-47 (`register_notifier`, `unregister_notifier`) และ 134 (`_notify_low_stock`)**:
   - **รายละเอียด:** กลไก Observer Pattern ในการส่งสัญญาณแจ้งเตือนผู้จัดการเมื่อสต็อกสินค้าต่ำกว่า threshold
   - **ความเสี่ยง:** หากเกิดความผิดพลาดในการกระจายข้อความแจ้งเตือน (เช่น แจ้งเตือนไม่ส่ง หรือเกิด exception ค้าง) ผู้จัดการจะไม่ทราบว่าสินค้าขาดสต็อก ส่งผลให้สั่งของมาเติมไม่ทันเวลา
3. **`src/service.py` บรรทัด 140-148 (`stock_value_report`)**:
   - **รายละเอียด:** ฟังก์ชันสร้างรายงานมูลค่าสต็อกสินค้ารวมแยกตามหมวดหมู่
   - **ความเสี่ยง:** หากระบบคำนวณมูลค่าผิด เช่น คูณราคาต่อหน่วยกับจำนวนสต็อกผิดพลาด หรือจัดกลุ่มหมวดหมู่สินค้าผิด ฝ่ายบัญชีและการเงินจะนำข้อมูลตัวเลขที่ผิดพลาดไปใช้ในการปิดงบและการวางแผนสต็อก

---

## 2. ทำไม coverage 100% ถึงยังไม่ได้แปลว่า test ดี (ยกตัวอย่างจากโค้ดของตัวเอง)

**เหตุผลเชิงหลักการ:**  
Code Coverage วัดเพียง **"ปริมาณบรรทัดที่ถูกรัน (Line Execution)"** แต่ไม่ได้วัด **"คุณภาพของการตรวจสอบ (Assertion Quality)"** หรือความครอบคลุมของตรรกะทางธุรกิจ (Business Logic / State Invariants)

**ตัวอย่างจากโค้ดของระบบเรา:**  
ในเมธอด `sell` ของ `InventoryService`:
- หากเราเขียน test แบบธรรมดา:
  ```python
  def test_sell_simple():
      inv.sell("สายไฟ 2.5 sq.mm", 5)
  ```
  โค้ดจะรันผ่านบรรทัด `product.quantity -= quantity` และ `return OrderResult(...)` ครบทุกบรรทัด ทำให้รายงาน Code Coverage ขึ้นตัวเลข **100%** ทันที
- **แต่ในความเป็นจริง:** Test นี้ไม่ได้ใส่ `assert` ตรวจสอบเลยว่าสต็อกเหลือ 15 จริงหรือไม่, ไม่ได้ตรวจว่า `OrderResult` คืนค่าถูกต้องหรือไม่, และไม่ได้ทดสอบกรณีค่าขอบ (เช่น ขายจนเหลือ 0, ขายเกินสต็อก, หรือการส่งค่าลบ)  
ดังนั้น Coverage 100% จึงเป็นเพียงภาพลวงตาหากปราศจาก Assertions ที่รัดกุมและ Edge Cases ที่เพียงพอ

---

## 3. ถ้ามีเวลาเขียน test เพิ่มได้แค่ 3 ข้อ จะเลือกเขียนข้อไหนก่อน เพราะอะไร

1. **อันดับ 1: Test การแจ้งเตือนของ Observer Pattern เมื่อขายสินค้าจนสต็อกต่ำกว่า threshold**  
   - *เหตุผล:* เป็น Core Business Requirement (US-02) หากขายสินค้าจนสต็อกลดต่ำกว่าเกณฑ์แล้ว Observer ไม่ทำงานหรือส่งข้อความผิดพลาด ธุรกิจจะเสียหายจากการขาดสินค้าจำหน่าย
2. **อันดับ 2: Test ฟังก์ชันรับสินค้าเข้าคลัง (`receive`) พร้อมตรวจสอบ `StockTransaction`**  
   - *เหตุผล:* สต็อกที่ถูกต้องเริ่มต้นจากการรับเข้า (Inbound Flow) หากฟังก์ชัน `receive` ทำงานไม่ถูกต้อง ยอดสต็อกตั้งต้นจะผิดทั้งหมด และการตรวจสอบประวัติย้อนหลัง (Audit Trail) จะล้มเหลว
3. **อันดับ 3: Test ฟังก์ชันรายงานมูลค่าสต็อก (`stock_value_report`) ในกรณีมีสินค้าหลายหมวดหมู่และหมวดหมู่ว่าง**  
   - *เหตุผล:* เป็นฟังก์ชันสำคัญในการสรุปภาพรวมทางการเงิน (US-03) ที่ช่วยให้ผู้บริหารเห็นมูลค่าสินค้าคงคลังที่แท้จริง
