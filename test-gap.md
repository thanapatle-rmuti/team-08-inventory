# บันทึกการวิเคราะห์ Test Gap (ขั้นที่ 4)

## โจทย์
ขอให้ AI เขียน unit test สำหรับเมธอด `sell` ของคลาส `Inventory` ด้วย prompt สั้น ๆ:
> *"ช่วยเขียน pytest unit test สำหรับเมธอด sell ของ class Inventory ให้หน่อย"*

ผลลัพธ์ที่ AI มักตอบกลับมาคือ Unit Test ที่ครอบคลุมเฉพาะ **Happy Path** (กรณีปกติที่ส่งข้อมูลถูกต้องและสต็อกพอ) ทำให้ test ผ่านง่ายเกินไปและสร้างความเข้าใจผิดว่าโค้ดปลอดภัยแล้ว ทั้งที่ยังไม่ได้ทดสอบกรณีขอบ (Edge Cases), กรณีข้อมูลผิดพลาด, หรือ Error Path ที่อาจเกิดขึ้นจริงในระบบ

---

## ตารางเปรียบเทียบ Test Gap (3 คอลัมน์)

| กรณีที่ AI ให้มา (Baseline) | กรณีที่ขาด (Gap Analysis) | Test ที่เราเขียนเสริม (Extended Tests) |
|---|---|---|
| **กรณีปกติ (Happy Path):** ขายสินค้าจำนวนปกติที่น้อยกว่าสต็อก และสต็อกลดลงถูกต้อง (`test_sell_basic_happy_path`) | **[กลุ่มค่าขอบ]** การขายสินค้าจนหมดสต็อกพอดี (จำนวนที่ขาย = สต็อกทั้งหมด) สต็อกต้องเหลือ 0 พอดี และไม่เกิด error | `test_sell_exact_stock_remaining_zero`<br>- ตรวจสอบว่าเมื่อสต็อกมี 5 ขาย 5 สต็อกคงเหลือต้องเป็น 0 พอดี |
| *(ไม่มี)* | **[กลุ่มค่าที่ไม่ควรรับ]** การส่งจำนวนเป็น 0 เข้ามา (`quantity = 0`) ระบบต้องไม่ยอมรับและไม่ตัดสต็อก | `test_sell_zero_quantity_raises_value_error`<br>- Assert ให้ระบบโยน `ValueError("จำนวนต้องมากกว่า 0")` |
| *(ไม่มี)* | **[กลุ่มค่าที่ไม่ควรรับ]** การส่งจำนวนติดลบ (`quantity < 0`) ซึ่งอาจทำให้เกิดช่องโหว่เพิ่มสต็อกโดยไม่ได้รับเข้า | `test_sell_negative_quantity_raises_value_error`<br>- Assert ให้ระบบโยน `ValueError("จำนวนต้องมากกว่า 0")` |
| *(ไม่มี)* | **[กลุ่มค่าที่ไม่ควรรับ]** การสั่งซื้อเกินจำนวนสต็อกที่มีอยู่จริงในคลัง | `test_sell_exceeds_stock_raises_value_error`<br>- Assert ให้ระบบโยน `ValueError("สต็อกไม่เพียงพอ")` และสต็อกต้องไม่ถูกหัก |
| *(ไม่มี)* | **[กลุ่มเส้นทาง Error]** การพยายามขายสินค้าที่ไม่มีชื่ออยู่ในระบบคลังสินค้า | `test_sell_non_existent_product_raises_key_error`<br>- Assert ให้ระบบโยน `KeyError` พร้อมระบุชื่อสินค้าที่ไม่พบอย่างชัดเจน |
| *(ไม่มี)* | **[กลุ่มชนิดข้อมูล]** การส่งชนิดข้อมูลผิดประเภท เช่น ทศนิยม (`float: 2.5`) หรือข้อความ (`str: "ห้า"`) | `test_sell_invalid_type_float_raises_type_error`<br>`test_sell_invalid_type_string_raises_type_error`<br>- Assert ให้ระบบโยน `TypeError("จำนวนต้องเป็นตัวเลขจำนวนเต็ม")` |

---

## บทเรียนสำคัญจากการวิเคราะห์ (Key Takeaways)
1. **AI มีแนวโน้มทำ Happy-Path Bias**: AI มักสร้าง test ที่ออกแบบมาให้ "ผ่านง่าย" เพราะตอบสนองต่อ prompt ที่ไม่ได้ระบุ constraint หรือ exception handling
2. **จำนวน Test มากไม่ได้แปลว่าปลอดภัย**: Test ที่มีเพียงแค่การเรียกใช้ฟังก์ชันแล้ว assert ว่าไม่พัง หรือตรวจเฉพาะค่าปกติ ไม่สามารถการันตีความทนทาน (Robustness) ของระบบได้
3. **การออกแบบ Assert ที่มีความหมาย**: ต้องตรวจสอบทั้ง side-effect (เช่น สต็อกต้องคงเดิมเมื่อเกิด error) และ exception type พร้อม error message ที่ถูกต้อง เพื่อให้เป็น Contract ที่แท้จริง
